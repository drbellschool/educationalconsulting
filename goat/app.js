const e=document.getElementById('app');
e.className='shell';
e.innerHTML='<h1>Goat Bot</h1><div class="card"><h3>Status</h3><p>Dashboard script restored. USDA data file is updating. Next step is reconnecting the live cards.</p></div>';
