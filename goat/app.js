fetch('data/goat-bot.json?v='+Date.now()).then(r=>r.json()).then(d=>{let i=d.inventory||{};app.innerHTML='<h1>Goat Bot</h1><div class=card>Status '+(d.ok?'LIVE':'WATCH')+'<br>API '+d.status+'<br>Data '+d.bytes+'<br>Inventory '+(i.head||0)+'/'+(i.capacity||40)+'</div>'})